--- reload ---
reload [equipment-name] : reloads the configuration.

  Without parameter, the reload command reloads all the configuration of Jtacl.

  If an equipment-name is specified, Jtacl reloads the configuration of this
equipment. The topology is also reloaded and rebuilt.

Example:
--------

  jtacl> reload

  jtacl> reload router1
