--- reload ---
reload [equipment-name] : reloads the configuration.

  Without parameter, the reload command reloads all the configuration of lsfw.

  If an equipment-name is specified, lsfw reloads the configuration of this
equipment. The topology is also reloaded and rebuilt.

Example:
--------

  lsfw> reload

  lsfw> reload router1
